# 测试报告（2026-09-03）

## 一、基本信息

| 项目 | 内容 |
|---|---|
| 测试日期 | 2026-09-03 |
| 代码基线 | commit `bf17e0f`（feat: 游客成绩云同步，登录后上传本地更优成绩） |
| 运行环境 | Windows 10.0.26200 x64 · Node v24.15.0（项目要求 ≥22.5，满足） |
| 后端框架 | node:test + supertest，`:memory:` 内存库，每用例全新 app |
| 前端框架 | vitest 4.1.11，fetch 以 `vi.stubGlobal` mock，测试与源码同目录 |
| 执行方式 | 根目录 `npm test`（= 后端 `node --test` → 前端 `vitest run`） |

## 二、结果摘要

| 维度 | 后端 | 前端 | 合计 |
|---|---|---|---|
| 功能用例 | 20 | 22 | **42** |
| 通过 | 20 | 22 | **42** |
| 失败 / 跳过 | 0 / 0 | 0 / 0 | **0** |
| 耗时 | ≈1.03 s | ≈0.40 s | ≈1.43 s |

> 注：node:test 汇总计数为 21，其中 1 条是 `test/helpers.js` 的文件级条目（非功能用例），故实际功能用例 20 个。

**结论：全部通过。**

## 三、后端用例明细（20 个）

### auth.test.js — 注册 / 登录 / 会话（10 个）

| 用例 | 验证点 | 结果 |
|---|---|---|
| 注册成功返回 token 与用户名 | 200 · `{ok,token,username}` 结构 | ✅ |
| 重复用户名注册返回 409 | 用户名唯一约束 → 409 | ✅ |
| 非法用户名 / 短密码注册返回 400 | 1 位用户名、3 位密码均被拒 | ✅ |
| 登录成功 / 密码错误 / 用户不存在 | 成功返 token；两种失败统一 401，不区分原因（防撞库探测） | ✅ |
| 未登录访问受保护接口返回 401 | requireAuth 中间件兜底 | ✅ |
| 退出登录后令牌失效 | logout 后原 token 立即 401 | ✅ |
| 过期会话返回 401，且在下次登录时被清理 | **锁定「认证路径只读」设计**：过期会话查询时不删除，由登录时低频清理回收（用例直查库断言会话数） | ✅ |
| 未过期会话正常使用 | 正常令牌可通过鉴权 | ✅ |
| 剩余不足 15 天的会话自动续期到约 30 天 | 滑动续期生效（断言剩余 >28 天） | ✅ |
| 剩余超过 15 天的会话不触发续期 | 不续期（断言剩余在 15~25 天之间） | ✅ |

### records.test.js — 成绩提交（7 个）

| 用例 | 验证点 | 结果 |
|---|---|---|
| 首次提交成绩成为纪录 | `isNewRecord: true` · `best` 回传本次成绩 | ✅ |
| 更差的成绩不覆盖纪录 | `isNewRecord: false` · `best` 保持旧值 | ✅ |
| 更好的成绩覆盖纪录 | upsert 生效 | ✅ |
| GET /api/records 返回各难度最佳 | map 结构 `{ "3": 5, "5": 15 }` | ✅ |
| 无效难度 / 无效成绩返回 400 | size=7、time=-1 被拒 | ✅ |
| 低于该难度下限的成绩返回 400 | 5×5 提 0.01s 被拒、3×3 提 0.2s 被拒；**边界值** 5×5 恰好 1.2s（= 极限下限）允许通过 | ✅ |
| 每分钟最多 10 次提交，超出被限速 | 第 11 次 429 | ✅ |

### leaderboard.test.js — 排行榜（3 个）

| 用例 | 验证点 | 结果 |
|---|---|---|
| 排行榜按成绩升序排列 | best_time 升序 | ✅ |
| 同一用户各难度独立排名，最多返回 10 人 | 按 size 隔离 · LIMIT 10 | ✅ |
| 游客可查看排行榜，非法难度返回 400 | 公开接口语义 · 参数校验 | ✅ |

## 四、前端用例明细（22 个）

### state.test.js — 游客记录持久化与会话状态（7 个）

| 用例 | 验证点 | 结果 |
|---|---|---|
| 保存后可读取 | localStorage 往返一致 | ✅ |
| 非法或超范围的值被过滤 | 负数 / 非数字 / 超 3600s / 非法难度 2 均被滤掉 | ✅ |
| 损坏的数据返回空对象 | 非 JSON 容错 | ✅ |
| 登录时云端记录与游客记录合并取最优 | mergeRecords 取各难度最小值 · showAuth 关闭 | ✅ |
| 退出登录后恢复游客记录 | token/user 清空 · records 回退本地 · 弹登录框 | ✅ |
| 无 token 时初始 records 取自本地游客记录 | 模块初始化语义（vi.resetModules + 动态 import） | ✅ |
| 有 token 时初始 records 为空，待服务器拉取 | 同上 | ✅ |

### api.test.js — fetch 封装（5 个）

| 用例 | 验证点 | 结果 |
|---|---|---|
| 网络失败时返回友好错误 | 归一为「无法连接服务器…」 | ✅ |
| 自动附带 x-token 请求头 | 从 state 读取并注入 | ✅ |
| 401 时清除会话并要求重新登录 | 登录态：清 token + 弹登录框 | ✅ |
| 游客收到 401 不弹登录框 | **锁定游客 401 静默设计** | ✅ |
| POST json 请求体正确序列化 | method/body 均正确 | ✅ |

### sync.test.js — 游客成绩云同步（6 个，当前未提交代码的配套用例）

| 用例 | 验证点 | 结果 |
|---|---|---|
| 无 token 时不发请求 | 游客不同步 | ✅ |
| 无服务器记录参数时不发请求 | 防御性入参校验 | ✅ |
| 无本地游客记录时不发请求 | 同上 | ✅ |
| 本地更优的成绩被提交，并采纳服务器返回的最优 | **只有严格更优才提交**（持平不同步，断言仅 1 次请求）；采纳回包 `best` | ✅ |
| 服务器缺失的难度会被补齐 | 服务器空记录 → 本地成绩全部上传 | ✅ |
| 不优于服务器的成绩不同步 | 零请求 | ✅ |

### gameLogic.test.js — 纯函数（4 个）

| 用例 | 验证点 | 结果 |
|---|---|---|
| shuffled 对 1..n 每个数字恰好出现一次 | 完整性（含非平方数 n=9） | ✅ |
| 多次运行并非总是一致 | 洗牌随机性 | ✅ |
| ratingFor 按每格秒数分档 | 四档评分文案 | ✅ |
| fmt 保留两位小数 | 计时格式化 | ✅ |

## 五、后端覆盖率（node --test --experimental-test-coverage）

| 文件 | 行覆盖 | 分支覆盖 | 函数覆盖 | 未覆盖位置 |
|---|---|---|---|---|
| auth.js | 100% | 100% | 100% | — |
| routes/authRoutes.js | 100% | 61.9% | 100% | 部分校验分支（如缺字段请求体、logout 无 token） |
| routes/recordRoutes.js | 100% | 93.75% | 100% | 个别边界分支 |
| app.js | 90.3% | 100% | 33.3% | **26-28 行：错误处理中间件**（无用例触发 5xx） |
| db.js | 92.7% | 60% | 100% | **37-39 行：旧库 sessions 表迁移分支**（测试库均为新建，列已存在） |
| **合计** | **97.7%** | **85.5%** | **92.6%** | |

前端未配置覆盖率工具（`@vitest/coverage-v8` 未安装），本次无前端覆盖率数据。

## 六、覆盖缺口与建议

1. **错误处理中间件无 5xx 用例**（app.js 26-28 未覆盖）：建议补一个「内部抛错 → 500 且返回统一 JSON、不泄漏堆栈」的用例。
2. **旧库迁移分支未覆盖**（db.js 37-39）：可用预置无 `expires_at` 列的库文件建库验证迁移回填逻辑。
3. **authRoutes 分支覆盖偏低**（61.9%）：主要缺请求体缺字段、logout 无 token 等畸形输入组合。
4. **前端覆盖率未配置**：如需数据，安装 `@vitest/coverage-v8` 后跑 `vitest run --coverage` 即可。
5. 游戏交互层（SchulteBoard 的计时、误点、作废逻辑）无组件级测试，属已知取舍——核心纯函数已抽到 gameLogic.js 并覆盖。

## 七、复现方式

```bash
npm install                 # 首次
npm test                    # 后端 node --test → 前端 vitest run
# 后端覆盖率：
cd backend && node --test --experimental-test-coverage
```
