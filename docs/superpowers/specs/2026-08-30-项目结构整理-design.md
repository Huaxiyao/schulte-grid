# 项目结构整理 - 设计文档

日期：2026-08-30

## 背景

后端启动方式已从 `start.bat` 脚本迁移到 IDEA / npm 直接启动，脚本失去存在价值；根目录残留两张开发过程截图；`.idea/` 工作区目录未被 git 忽略；项目缺少 README 说明启动方式。

## 变更清单

1. **删除文件**（git rm，git 历史中有备份）：
   - `start.bat`、`stop.bat`
   - `login-ui-v2-final.png`、`verify-final.png`

2. **`.gitignore` 追加**：`.idea/`

3. **新增 `README.md`**（根目录）：项目简介、启动方式（`npm install` → `npm run build` → `npm start`；开发 `npm run dev`）、数据库位置（根目录 `schulte.db`，运行时生成，已 gitignore）。

4. **更新过时文档**：`docs/superpowers/specs/2026-08-27-前后端分离-design.md` 中"一个 start.bat → 打开 3000 端口"等启动描述改为 npm 方式。`docs/superpowers/plans/` 下的实施计划属历史记录，不修改。

## 明确不做

- 不移动 `schulte.db`（保持根目录，不改代码）
- 不触碰工作区中未提交的 PWA 相关代码改动
- 不改 `docs/superpowers/plans/` 历史文档

## 提交策略

精确 `git add` 上述涉及文件，一条 chore 提交，避免卷入未提交的 PWA 工作。

## 验证

- `npm start` 服务正常启动，`http://localhost:3000` 可访问
- `git status` 中 PWA 相关改动（backend/frontend 下的 M 文件）未被卷入提交
